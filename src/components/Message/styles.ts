import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    messsageContent: {
        paddingVertical: 16,
        alignItems: 'center',
        gap: 8,
    },

    messageText: {
        textAlign: 'center',
        fontSize: 18,
        color: '#333333',
    },
})
